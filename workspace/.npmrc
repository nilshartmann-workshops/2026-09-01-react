; Ohne diese Zeile erzeugt ein npm, in dessen Konfiguration legacy-peer-deps=true
; steht, eine abweichende package-lock.json: Die Peer-Kante von @tailwindcss/vite
; auf vite fällt dann weg, und vite gilt samt esbuild und rollup als dev-only.
legacy-peer-deps=false
